# sankum.in (static)

Multi-page HTML/CSS/JS portfolio with shared header/footer via partials.

## Local preview
Use VS Code + Live Server (recommended).

## Deploy
GitHub repo → Settings → Pages → Deploy from a branch → main → /(root)

## Domain
Ensure CNAME contains sankum.in and enable HTTPS in Pages settings.
